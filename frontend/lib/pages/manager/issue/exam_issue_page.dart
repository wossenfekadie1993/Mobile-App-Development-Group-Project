import 'package:flutter/material.dart';

class ExamIssuePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exam Issue'),
      ),
      body: Center(
        child: Text('Exam Issue Page'),
      ),
    );
  }
}
