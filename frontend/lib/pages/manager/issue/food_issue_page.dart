import 'package:flutter/material.dart';

class FoodIssuePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Food Issue'),
      ),
      body: Center(
        child: Text('Food Issue Page'),
      ),
    );
  }
}
