import 'package:flutter/material.dart';

class GenderIssuePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gender Issue'),
      ),
      body: Center(
        child: Text('Gender Issue Page'),
      ),
    );
  }
}
