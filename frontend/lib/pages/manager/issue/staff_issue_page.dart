import 'package:flutter/material.dart';

class StaffIssuePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Staff Issue'),
      ),
      body: Center(
        child: Text('Staff Issue Page'),
      ),
    );
  }
}
